# CircleCI 2.0 configuration file
#
version: 2
jobs:
  build:
    docker:
      # specify the version
      image: yangyw12345/arch-builder:latest

    steps:
      - checkout cgen-llvm
      - run: 
          name: build
          command: mkdir build && cd build && cmake .. && make
      - run: 
          name: test
          command: cd ./bin && bash ./tests.sh
